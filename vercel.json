{
  "version": 2,
  "builds": [
    {
      "src": "app.py", // Replace "app.py" with the name of your main Python file
      "use": "@vercel/python"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/app.py" } // Replace "app.py" with the name of your main Python file
  ]
}
